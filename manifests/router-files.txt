# nftables (persistent firewall)
 /etc/nftables.conf

# Netplan (interfaces, VLAN subinterfaces later)
 /etc/netplan/

# Sysctl (ip_forward, rp_filter, etc)
 /etc/sysctl.conf
 /etc/sysctl.d/

# systemd networking related (optional later)
 /etc/systemd/system/

# resolv / dns (optional; careful if managed dynamically)
 /etc/resolv.conf
